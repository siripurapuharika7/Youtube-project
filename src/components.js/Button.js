const Button=({name})=>{
    // const{names}=props;
   
    
    return(
        <div>
        <button className="bg-gray-200 mx-5 py-2 px-5 rounded-lg">{name}</button>
        </div>

    );
};
export default Button;